#include<iostream>
using namespace std;
int main()
{
    int age,g;
    cin>>age>>g;
    if (g>2 || g<1)
    {
        cout<<"Invalid";
    }
    else if(age<25 && g=1)
    {
        cout<<"The person belongs to Group 1.";
    }
    else if(age>25 && age<25 && g=1)
    {
        cout<<"The person belongs to Group 3."
    }
    else if(age<25 && g=2)
    {
        cout<<"The person belongs to Group 2.";
    }
    else if(age>25 && age<25 && g=2)
    {
        cout<<"The person belongs to Group 2."
    }
    else if(age>45)
    {
        cout<<"The person belongs to Group 5."
    }
}
